using UnityEngine;
using System.Collections;

public class nutsack : MonoBehaviour {

	// Use this for initialization
	void Start () {
        int angle = Random.Range(0, 360);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
